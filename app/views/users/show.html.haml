- if current_user and current_user==@user
  - if current_user.email == 'robot@vaope.com'
    .alert-message.warning
      %a.close{:href => '#'}×
      %p
        %strong
          Debes cambiar tu correo para poder enviarte notificaciones :(
.row
  .span11.columns
    %h3
      Locales de
      = @user.name
    %ul.places
      - @user.places.each do |place|
        = render :partial => 'shared/place', :locals => {:place => place}
  .span5.columns
    .block
      %img.user_avatar.large{:src => @user.avatar}
      %h2.inline= @user.name
    - if current_user and current_user != @user
      .block.actions
        %a.btn.primary{:href => friend_user_path(@user)} Sé su amigo